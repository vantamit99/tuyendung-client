export enum AUTH_ENDPOINT {
    LOGIN = 'api/auth/login',
    LOGOUT = 'api/auth/logout',
    PROFILE = 'api/auth/profile',
    REFRESH_TOKEN = 'api/auth/refresh-token',
    RESET_PASSWORD = 'api/auth/reset-password',
}
export enum USER_ENDPOINT {
    USER = 'api/admin/users'
}
export enum ROLE_ENDPOINT {
    ROLE = 'api/admin/roles'
}
export enum PERMISSION_ENDPOINT {
    PERMISSION = 'api/admin/permissions'
}
export enum CATEGORY_ENDPOINT {
    CATEGORY = 'api/categories'
}
export enum AREA_ENDPOINT {
    AREA = 'api/areas'
}
export enum POSTS_ENDPOINT {
    POSTS = 'api/posts'
}
export enum CV_ENDPOINT {
    CV = 'api/cv'
}